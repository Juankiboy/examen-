<%= form_for(@juan) do |f| %>
  <% if @juan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@juan.errors.count, "error") %> prohibited this juan from being saved:</h2>

      <ul>
      <% @juan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_area :nombre %>
  </div>
  <div class="field">
    <%= f.label :dirrecion %><br>
    <%= f.text_area :dirrecion %>
  </div>
  <div class="field">
    <%= f.label :pedido %><br>
    <%= f.text_area :pedido %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
